main(a)
{
    x=10.0f;
    y=50.0f;
    x=x*y;
    printf(x);
    x=x-y;
    printf(x);
    z=0.0f;
    z=x;
    printf(z);
    a=1;
    b=2;
    printf(a+b);
    c=(float) a;
    c=c+y;
    printf(c);


    u=new int[5];
    u[0]=1;
    u[4]=8;
    showarr(u);

    return(x);
}